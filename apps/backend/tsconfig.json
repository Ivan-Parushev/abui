{
    "extends": "tsconfig/nodejs.json",
    "include": ["./src/**/*.ts"],
    "exclude": ["node_modules", "test", "dist", "**/*spec.ts"],
    "compilerOptions": {
        "outDir": "./dist",
        "baseUrl": "./"
    }
}
